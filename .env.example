# Copy this file to .env and fill in real values. .env is gitignored.

POSTGRES_USER=text2sql
POSTGRES_PASSWORD=changeme
POSTGRES_DB=text2sql
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Read-only role created in Giai đoạn 5 (see PROGRESS.md) — used by the app
# to connect for query execution, never the superuser above.
POSTGRES_READONLY_USER=readonly_app
POSTGRES_READONLY_PASSWORD=changeme

OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_SQL_MODEL=qwen2.5-coder:7b
OLLAMA_EMBED_MODEL=nomic-embed-text

CHROMA_PERSIST_DIR=./chroma_data
